/* 

    Template String

    let gols = 1000;
    let frase1 = "Romario fez " + gols + " gols ";
    let frase2 = "Romario fez ${gols} gols"; - Utilizando Template String
*/

// Declare uma variável contendo uma string
    let comidaFavorita = "Pizza";

// Declare uma variável contendo um número dentro de uma string
    let numeroString = `Meu carro tem ${4} pneus`;
    console.log(numeroString);

// Declare uma variável com a sua idade
    let idade = 23;   
    
// Declare duas variáveis, uma com seu nome
// e outra com seu sobrenome e some as mesmas
    let nome = "Gabriel";
    let sobrenome = "Lima";
    let nomeCompleto = nome + " " + sobrenome;

// Coloque a seguinte frase em uma variável: It's time
    let time = "It's time"

// Verifique o tipo de variável que contém o seu nome
    console.log(typeof nomeCompleto)